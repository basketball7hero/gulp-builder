extends ../layouts/default/template


block head
    - pageSettings['pageTitle'] = 'Вход';
    - pageSettings['pageDescription'] = 'Вход';
    - pageSettings['pageKeywords'] = 'Вход';
    - pageSettings['class'] = '_login';
    - pageSettings['navbar'] = true;
    - pageSettings['breadcrumbs'] = [{ text: 'Вход', active: true }];


block body-top-scripts


block body-bottom-scripts
    script(src='/static/js/bundle.min.js')


block content
    .container-fluid
        .container
            +e.content.grid-12
                +e.col.col-9
                    section.section
                        +b.FORM.form.js-form(id='form-user.login-1' data-action="user.login" autocomplete="off")
                            +e.section
                                input(data-name='email' type='text' placeholder='email' autocomplete='off')
                            +e.section
                                input(data-name='password' type='password' placeholder='password' autocomplete='off')
                            +e.section
                                button(type='submit') Отправить
                +e.col.col-3
                    section.section
                        | test
